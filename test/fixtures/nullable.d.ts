declare module goog.functions {

    /**
     * @param {?number} num
     */
    export var foo: (num: number) => void;

    /**
     * @param {!Date} date
     */
    export var bar: (date: Date) => void;
}
